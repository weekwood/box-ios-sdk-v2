Several changes to ODRefreshControl were made to incorporate it into BoxSDK:

* typedefs, constants, and classes had their names prefixed with "Box".
* #defines, functions, include Box in their names.
